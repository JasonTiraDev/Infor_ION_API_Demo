@page "/Items"
@inject IConfiguration _config

<PageTitle>Items</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Class="my-4">
    <!-- Page Header -->
    <MudPaper Class="pa-4 mb-4 mx-auto" Elevation="5" MaxWidth="800px">
        <MudText Typo="Typo.h4" Align="Align.Center" Class="mb-2">
            Items List
        </MudText>
        <MudText Typo="Typo.subtitle1" Align="Align.Center">
            List of items pulled from ION API - CSI
        </MudText>
        <!-- Search Field -->
        <MudTextField @bind-Value="searchString" Placeholder="Search items..."
                      Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search"
                      Class="mt-4" />
    </MudPaper>

    <!-- Items Table -->
    <MudPaper MaxHeight="800px" MaxWidth="800px" Class="mx-auto" Elevation="5">
        <MudTable Items="FilteredItems" Hover="true" FixedHeader="true" Dense="true">
            <HeaderContent>
                <MudTh>Item</MudTh>
                <MudTh>Description</MudTh>
                <MudTh>ItemId</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Item">@context.Item</MudTd>
                <MudTd DataLabel="Description">@context.Description</MudTd>
                <MudTd DataLabel="ItemId">@context._ItemId</MudTd>
            </RowTemplate>
            <PagerContent>
                <MudTablePager PageSizeOptions="new int[]{5, 10, 25, 50, 100}" />
            </PagerContent>
        </MudTable>
    </MudPaper>
</MudContainer>
